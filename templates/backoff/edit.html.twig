{% extends 'base.html.twig' %}

{% block title %} Editer une salle {{ parent() }}{% endblock %}

{% block body %}

    <div class="container">
        {{ form_start(form) }}
            {{ form_rest(form) }}

            <button class="btn btn-success">Editer</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
