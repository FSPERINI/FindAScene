{% extends 'base.html.twig' %}

{% block title %}S'inscrire - {{ parent() }}{% endblock %}

{% block body %}

    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}
           <button class="btn btn-success">Confirmer votre inscription</button>
            {{ form_end(form) }}
    </div>
{% endblock %}