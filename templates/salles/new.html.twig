{% extends 'base.html.twig' %}

{% block title %}Ajouter une salle - {{ parent() }}{% endblock %}

{% block body %}

    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-success">Ajouter cette Salle</button>

        {{ form_end(form) }}
    </div>
{% endblock %}
