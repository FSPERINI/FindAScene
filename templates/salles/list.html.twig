{% extends 'base.html.twig' %}

{% block title 'Toutes les salles' %}
{% block stylesheets %}
    {{ parent() }}
    <link href="{{ asset('css/style.css') }}" rel="stylesheet" />
    <link href="{{ asset('css/styleSalles.css') }}" rel="stylesheet" />
{% endblock %}
{% block body %}
    <div class="container">
        {% for salle in salles %}
        
            <article class="card-salle">
                        <h2 class="titre-salle">{{ salle.NomSalle}}</h2>
                        <p class="ville-salle">{{ salle.ville}}</p>
                        <a href="{{ path('show_salles', {slug: salle.slug, id: salle.id} ) }}" class="white"><button class="enSavoirPLus">Voir la salle en détails</button></a>
            </article>
        {% endfor %}
    </div>
{% endblock %}
