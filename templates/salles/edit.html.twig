{% extends 'base.html.twig' %}

{% block title %}Modifier la Salle - {{ parent() }}{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
            {{ form_widget(form) }}

            <button class="btn btn-primary" formnovalidate>
                Modifier la salle
            </button>
        {{ form_end(form) }}
    </div>
{% endblock %}